{
    "name": "imos-receiver",
    "version": "1.0.0",
    "description": "IMOS XML Receiver Server — accepts XML orders from createSubSo and saves to the IMOS inbox folder",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "dev": "node --watch server.js"
    },
    "dependencies": {
        "express": "^4.21.0"
    }
}